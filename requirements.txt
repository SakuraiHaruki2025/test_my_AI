# 必要なライブラリ
# Core dependencies for LLM
torch>=2.0.0
transformers>=4.35.0
accelerate>=0.24.0
peft>=0.7.0  # Parameter-Efficient Fine-Tuning (LoRA, QLoRA)
bitsandbytes>=0.41.0  # 量子化のため

# Dataset and utilities
datasets>=2.14.0
sentencepiece>=0.1.99
protobuf>=3.20.0

# Optimization and monitoring
tqdm>=4.65.0
numpy>=1.24.0
pandas>=2.0.0

# Optional but recommended
scipy>=1.10.0
scikit-learn>=1.3.0
